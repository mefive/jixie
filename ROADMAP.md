# jixie 长期功能规划（ROADMAP）

> 2026-07-06 初版；2026-08-12 随项目目标变化重构。本文是功能优先级与执行状态的**唯一规划真相源**。
> 已完成工作的设计细节保留在 `docs/design/`、`docs/reports/` 与 git 历史中，不再把本文件扩成逐提交日志。
> 战略视野见 `roadmaps/`，学习范围见 `docs/learning-map.md`，底层取舍见 `docs/philosophy.md`。

## 状态图例

- ⬜ 待设计或待实施
- 🚧 已认领并进行中
- ✅ 已完成且有验证证据
- 💤 明确延后，未满足触发条件前不投入

## 北极星

jixie 是**个人多市场量化研究实验室**。当前目标是借真实系统建设系统学习量化投资，用数据、代码和
可复现证据研究中国内地、香港、美国的股票、债券与商品。

当前阶段的成功标准是：

1. 能把理论、论文或市场观点转化为明确、可反驳的量化假设；
2. 能用时点正确、口径清楚的数据复现并审查结论；
3. 能在横截面、时间序列、跨资产 Panel、宏观状态和组合风险五个层面使用合适的方法；
4. 能保留完整研究记录，区分探索、正式验证和最终产品能力；
5. 中文和英文使用者都能从帮助文档理解概念、完成操作并识别限制。

长期目标是在真实成本、严格样本外和可执行性约束下，找到具有持续费后正期望、良好 Sharpe / Calmar，
且回撤、尾部风险、容量和执行难度能够接受的策略组合。系统只能帮助寻找和验证，不能承诺稳定盈利；
Sharpe 与 Calmar 是结果指标，不是允许反复拟合历史的优化目标。

## 执行原则

1. **学习问题先于功能清单**：每项新增能力必须说明要理解什么理论、验证什么假设、产生什么可复查证据。
2. **广探索、窄产品化**：Agent、Python、脚本和临时分析可以灵活；只有反复使用或关系研究可信度的流程
   才进入正式页面、共享类型与数据库。
3. **统一研究纪律，不伪造统一市场**：多市场共享 PIT、holdout、成本和可复现标准，但身份、日历、
   时区、币种、公司行动、税费和交易制度分别建模。
4. **先语义，后数据，再结论**：新增市场或资产前先冻结字段、单位、可得时间、基准币种和缺失规则，
   不把“抓到了数据”视为完成。
5. **帮助与双语属于完成定义**：新增用户可见研究能力时，同步提供中文/英文文案、概念解释、操作路径和限制。
6. **实盘延后但可执行性前置**：当前不扩券商和自动下单；研究从一开始仍计入真实成本、容量和制度约束。
7. **AI 适用同一研究纪律**：Agent 发起的实验也计入尝试次数、受 holdout 和权限约束，不批量挖显著性。

---

## 已完成基础

以下能力构成新阶段的起点；这里只保留能力索引，完成实况见对应设计文档与测试。

### A. 可信研究与因子体系 ✅

- 股票横截面、单资产时间序列、跨资产 Panel、宏观状态四类 Factor V2 研究协议；
- code-first 因子、组合因子、不可变报告与发布、策略依赖血缘；
- Rank IC、分层、多空、费后、换手、中性化、相关性、holdout、多重检验提示；
- 横截面 Newey-West 与 Fama–MacBeth 稳健推断；
- 20 个通过项目准入纪律的预置股票因子及否决报告。

依据：`docs/design/factor-v2.md`、`docs/design/factor-research-discipline.md`、
`docs/design/factor-cross-sectional-inference.md`。

### B. 策略、回测与组合风险 ✅

- TypeScript / Python 策略表达，唯一 TypeScript Engine 负责撮合与市场规则；
- 股票、ETF、股指期货、条件单、日/周/月周期和常用技术指标；
- 参数、仓位与容量扫描，真实费用、滑点、冲击和历史可投资状态；
- 多资产收益贡献、相关性、市场风险暴露、宏观敏感度、Alpha/Risk 重合与压力情景。

依据：`docs/design/python-and-sandbox.md`、`docs/design/strategy-parameter-scan.md`、
`docs/design/asset-allocation-data.md`。

### C. 对话式数据分析基础 ✅

- 统一 Agent、多轮持久化与流式续接；
- 白名单只读 SQL、沙盒 `analyzeData`、SQL 图表和计算图卡片；
- Agent 可运行受约束的快速回测和正式探索段因子分析；
- 查询、代码和图表由确定性 spec 重跑，原始大数据不进入模型上下文。

依据：`docs/design/unified-agent.md`、`docs/design/agent-code-tool.md`、
`docs/design/computed-chart.md`、`docs/design/agent-research-loop.md`。

### D. 境内数据、产品与运行底座 ✅

- A 股、指数、ETF、股指期货、境内利率曲线、商品期货、宏观与部分外部风险驱动数据；
- 历史证券身份、风险警示/退市、PIT 财务、数据审计与幂等同步；
- 市场/因子气象、选股、因子、Lab、信号和公共研究资产库；
- 中英双语产品与公开帮助；
- 持久任务公平队列、生产 bootstrap、增量维护和备份编排。

依据：`docs/design/historical-investability.md`、`docs/design/production-maintenance.md`、
`docs/design/multi-user-engineering.md`、`docs/design/user-guide.md`。

### E. 信号与执行记录 ✅（冻结扩展）

- 每日信号、邮件提醒、模型/模拟/实际账户、人工成交回填、执行偏差和券商条件单清单已经打通；
- 现有能力保留并修复缺陷，但当前不增加自动券商通道，不以实盘运营作为近期验收目标。

依据：`docs/design/daily-signals.md`。

---

## 历史编号兼容说明

2026-08-12 以前的设计文档会保留“原 ROADMAP 1.x～7.x”编号，用来标识当时的实施批次；这些编号是
历史坐标，不再表示当前优先级，也不会为此批量改写已经完成的设计记录。大致对应关系如下：

| 原编号 | 历史主题 | 当前归档位置 |
|---|---|---|
| 1.x | 回测可信度与参数扫描 | 已完成基础 B |
| 2.x | 策略表达、订单与技术指标 | 已完成基础 B |
| 3.x | 因子研究与因子到策略 | 已完成基础 A / B |
| 4.x | 数据、工程与多用户底座 | 已完成基础 D |
| 5.x | 每日信号与执行记录 | 已完成基础 E |
| 6.x | 仓位、风险与交易纪律 | 已完成基础 B；后续深化见 4.x |
| 7.x | 统一 Agent 与数据分析 | 已完成基础 C；后续深化见 1.x |

引用旧编号的文档若未注明“原”，应按其文首日期和实施记录理解；新增设计只能引用本版主线编号。

---

## 当前主线一：响应式量化研究工作台

本主线是当前产品核心：服务愿意学习或已经掌握基础 Python、pandas 和统计学的个人量化研究者。Research
页面已经演进为 jixie 原生的响应式量化研究文档；Agent 负责协助取数、编码、排错、方法选择和解释，用户保留自由研究能力，平台负责数据语义、沙箱、执行约束和复现。

当前产品表面与开发里程碑以[响应式量化研究工作台](docs/design/reactive-quant-research-workbench.md)为准。

### 1.1 固定研究流程（已撤销）

2026-08-19 产品决策：早期四个固定统计流程、专用结果卡和确定性重跑入口无人使用，已经从正式产品表面、
Agent 工具与共享类型中删除。Research 不再要求用户把问题套入预登记流程，方法与公式写入 Markdown，实际计算
与诊断写入 Python。PIT-aware `UniverseSpec V1`、Measure Catalog、数据绑定和稳定对象身份继续保留，因为它们
是通用数据能力而不是固定统计流程。

### 1.2 旧固定流程运行记录（已撤销）

`ResearchStudy`、`ResearchRun`、`ResearchAttempt` 及父子重跑/专用比较已经删除。现在的可复现边界是
`ResearchDocument` 当前态与不可变 `ResearchExecution` 完整运行快照；用户需要复查时重新干净运行全文，
分别查看快照，不建设隐藏的结果层。

### 1.3 研究整理 Agent（Research Curator） ✅

单独的后台 Agent 增量梳理聊天和 turn trace，发现重复研究模式、临时计算、数据缺口、概念文档缺口和工具
问题，并用本地 schema、数据能力目录、Tushare 能力目录、代码、帮助和 ROADMAP 做只读核验。

它采用“确定性提取 → LLM 归纳 → 工具核验 → 人工分流”的闭环，不自动改库、抓取全量数据、修改 ROADMAP
或发布功能。先提供手动触发，经过建议接受率、重复率和核验错误率评估后，再增加默认每周增量摘要；每日运行
仅作为达到新对话阈值时的可配置选项。

**完成定义**：每条发现都给出原始证据、核验结果、重复情况、置信度和建议动作；用户可接受、拒绝、延后或
标记重复。权限严格限于当前用户，跨用户聚合必须匿名化并由管理员显式开启。

手动闭环已落地：按完成游标增量读取当前用户的研究消息和失败工具 trace，经严格 JSON
归纳后，用登记的 Research 能力、只读本地数据目录、版本化 Tushare 能力目录及其持久化探测结果做确定性核验，
并只读检索代码、帮助、ROADMAP 和设计文档。运行、原始证据、支持/限制核验依据、候选、跨运行指纹去重、人工
分流和独立的核验正确性反馈均持久化，并复用可恢复后台 Job。界面持续统计接受率、重复率和核验错误率，在至少
20 项已审候选和 20 项人工核验前只显示“积累样本”，不对质量作武断结论。

每周调度不是手动版遗留开发项：只有真实样本达到上述规模并完成质量复盘后才另行评估是否启用。每日触发保持
关闭，也不进入当前默认行为。Curator 始终不提供自动改库、全量同步、ROADMAP 修改、发布或转因子动作。

### 1.4 研究语义目录持续维护（长期 TODO）

数据结构和供应商能力增长时，持续扩充 Research 可以引用的对象、指标、序列、事件、加载器和 Python 使用兼容性，
避免出现“数据已经落库，但只有会写 SQL 的人才能使用”。通常新增语义目录项，不为每张表发明新 Spec 类型；
只有现有组合原语无法表达新的研究含义时才扩展 Spec，且不得暴露任意表名、字段名、JOIN 或 SQL 表达式。

**持续完成定义**：新增或实质改变一个研究数据域时，同一变更必须判断其公开语义，并补齐稳定 ID、单位、频率、
PIT/vintage/revision、确定性 loader、适用对象/变换、中英文说明和真实 fixture。纯存储优化不新增语义项；语义变化
即使不改表也必须升级版本。Research Curator 持续发现本地已有但目录不可达的数据缺口，人工确认后进入正常
开发流程。

已建立无向量的语义解析基础：版本化 `ResearchConceptRegistry` 管理概念、别名与不可替代关系；
`ResearchPlaybookRegistry` 通过 Tool 按需向 LLM 提供领域研究策略，它不是 Agent 框架 Skill；
`ResearchConceptBindingRegistry` 则作为平台内部审计白名单，把 Concept 映射到精确 Source、Measure、数据契约
和真实覆盖范围。结构化 `ConceptQuery` 只通过 Binding 解析语义概念，数据库字段词法搜索仅用于用户明确给出的
名称或稳定代码。首个 Playbook 与 Binding 切片覆盖黄金价格驱动，向量召回暂不引入。后续随真实问题持续增加
Concept、Playbook、Binding、loader 与 Python 研究示例；上述基础机制完成不代表 1.4 封板，1.4 始终是持续维护项。

本轮短期切片已闭环：美国 headline CPI 通过 BLS 官方公共 API 幂等回填到宏观底座，登记为精确的
`macro.inflation.us.cpi.headline` Concept/Binding，带保守 `availableDate`、latest-value backfill 披露、质量审计、
bootstrap 和每周维护。宏观 loader 以审计后的可用日对齐历史值，使其可用于带明确警告的探索性研究，而不把
回填值冒充历史实时 vintage。DXY 与 VIX 已完成精确数据源和使用权核验；当前供应商目录没有精确序列，官方
来源又要求外部许可，因此以版本化、带证据和下一步动作的 `blocked_by_source_rights` 状态呈现，不用 USD/CNH、
广义美元指数或其他波动率代理偷换概念。这代表当前黄金 Playbook 的短期数据切片收工，不改变 1.4 长期维护
属性。

当前 Research 多资产数据集切片已经完成：在不增加统计协议的前提下，把已有 `UniverseSpec` 的 A 股 PIT 股票池
规则开放为 Python `data.cross_section()`，并按月末重复同一规则形成 `data.panel()` 长表。两者必须使用固定公开
Schema、历史指数成分、上市/停牌/风险警示处理、数据 revision、明确资源上限和同一 SDK Contract 生成链；
IC、分层、衰减与换手继续归 FactorReport，不能在 Research 复制实现。

### 1.5 响应式量化研究文档 🚧

在现有 Research 页面内只提供 Markdown / Python Cell，以 Python AST 的变量定义与引用形成依赖 DAG。上游变化
将下游标记 `stale`；默认 lazy，不自动触发昂贵请求；只有干净 runtime 完整执行成功后才能显式封存不可变
`ResearchExecution` 快照。不引入 Jupyter，也不维护与 Research 平行的 Notebook 产品。

**M0–M3 已完成**：持久化文档、文档级自动保存、修订冲突保护、独立 Python runtime、Pyright 跨 Cell 语言
服务、平台 `data.series`、静态 SDK Contract、受控大表、Matplotlib artifact、原生 ECharts、依赖运行、中断、
Agent 开放变更会话、受控尝试、精确输出解释、干净全文运行、不可变快照、文档内历史和显式封存均已落地。
2026-08-19 进一步删除 Validation Cell、四个固定流程、专用报告和旧 Study/Run/Attempt 表；统计方法现在由
Markdown 公式/假设与 Python 成熟库实现共同表达。

**当前完成定义**：基础 Python 用户可以在 jixie 内完成平台取数、中等规模分析、静态与交互图、Agent 协作、
stale 依赖提示和干净环境固化。完整运行只证明这份源码在该环境产生了这些输出，不自动证明方法正确或结论可投资。

M4 第 1 项已经完成：成功封存的 `ResearchExecution` 可经 LLM 语义门和现有 Factor 编译器生成唯一的 Factor
草稿；草稿保留来源快照、revision/hash、提炼摘要和未解决限制，并可精确回跳来源。描述性研究、未来信息、
不受支持的数据或不能形成逐资产时点信号的研究会被拒绝；交接不会自动运行 FactorReport、揭示 Holdout 或发布。

M4 第 2 项已经完成：成功封存的 ResearchExecution 可经 LLM 语义门生成唯一、可审查的 Strategy 草稿，默认
使用 Python `py-v1`；草稿必须通过编译与受限运行时校验，保留来源快照、revision/hash、摘要、待验证项和精确
回链，但不会自动运行回测，用户仍须在 Strategy Lab 中确认参数并显式回测。

M4 第 3 项已经完成：Factor 已增加 Python SDK、`py-v1` 受限运行时、FactorReport / 发布 / Strategy 消费链路和
Monaco/Pyright 支持；现有 TypeScript Factor 保持兼容，新的 Factor 以及 Research → Factor 草稿默认 Python。
实现已按契约与持久化、横截面运行时、时序/Panel 下游闭环、编辑器与 E2E 四个切片落地。

Research 对派生结果的二次分析进入 backlog：FactorReport 可作为用户权限内的不可变 `results` 数据集读取；
Strategy 当前只有会被后续运行覆盖的 `lastResult`，精确历史复盘必须先引入不可变 `BacktestReport`，记录配置/
代码/结果 hash、完成时间、净值和成交快照。两类结果均不和 Research 建关系表，完整持仓历史也必须由引擎真实
产出后再开放。全局封存档案/搜索、底层数据副本、数据请求指纹和快照
自动归因等待真实需求，不阻塞 M3 收工。

### 1.6 研究方法模板与统计能力扩展 💤

先完成 1.5 框架，不以一次覆盖统计学目录为阻塞条件。以下进入 backlog，由真实研究问题、方法审计和数据
准备逐项触发：横截面 IC/分层/衰减/换手/容量、Fama–MacBeth/Panel、平稳性/协整、Walk-forward、Bootstrap、
参数稳定性、多重检验/FDR、组合与风险归因、情景压力测试、回测成交/成本/容量/实际偏差诊断。

已有 FactorReport、组合风险或回测实现必须复用共同计算内核；不能在 Research 复制一套口径不同的统计代码。

---

## 当前主线二：中英双语学习系统

### 2.1 从帮助中心到学习路径 ⬜

现有帮助中心已经覆盖产品操作和大量概念，但仍以“会使用页面”为主要组织方式。下一步按
`docs/learning-map.md` 建立可循序学习的入口，同时保留按页面查问题的手册结构。

第一阶段至少形成：

- 统计与研究偏差；
- 股票、债券、商品的收益与风险语言；
- 因子、回测、组合构造与绩效评价；
- 每个主题的概念文章、一个项目内真实例子、一个可复现练习和关联页面；
- 中文与英文内容同步设计，允许分批发布，但不能长期只有单一语言完成态。

**完成定义**：新学习者可以从入口选择一条路径，知道前置知识、在系统哪里实践、怎样判断练习完成；
帮助文章不把历史结果写成收益承诺，也不以截图替代方法说明。

### 2.2 复现研究目录 ⬜

建立“学过什么、复现过什么、结论是什么”的目录，而不是继续扩大预置因子菜单。每项记录关联理论来源、
数据可得性、实现、正式报告、否决原因和适用市场。

**完成定义**：先以少量代表性研究贯通目录；失败和无法复现的研究与成功项同样可查。目录是学习索引，
不按收益排名，不形成策略广场或跟单入口。

---

## 当前主线三：多市场、多资产数据地基

### 3.1 跨市场数据契约与数据源矩阵 ✅

在接入更多数据前，先为中国内地、香港、美国及股票、债券、商品冻结共同契约和领域差异。

契约至少包括：

- 稳定证券身份、资产类别、交易所/市场、上市/退市与代码变更；
- 交易日历、时区、夏令时、交易日归属和跨市场信息可得顺序；
- 交易币种、报价币种、汇率来源、本币收益与统一基准币收益；
- 分红、拆并股、配股等公司行动及 total-return / adjusted-price 语义；
- 财务公告日、宏观 vintage、数据修订和 `availableDate`；
- 数据许可、历史深度、退市覆盖、PIT 能力、限频、价格与可审计性。

**完成定义**：形成数据契约设计与数据源决策矩阵，并用至少一个中/港/美股票、一个债券序列和一个商品序列
验证字段能够表达真实差异。数据源选择必须单独核实当时的官方能力与许可，不在 ROADMAP 里拍脑袋锁厂商。

已落地版本化 DataContract / SourceDecision / Fixture 注册表：稳定身份、生命周期、时区与 DST、交易日归属、
跨市场可得顺序、币种与 FX、公司行动、total return、宏观 vintage、修订和 `availableDate` 均为显式字段。
现有 Concept Binding 从同一注册表派生契约，Research Catalog 向 Agent 暴露集成/计划边界，Research Curator
也用同一矩阵核验后续数据建议。`000001.SZ`、`00001.HK`、`AAPL`、中债 10 年国债曲线和 `AU.SHF`
fixture 已验证真实市场差异；港美股来源只登记为已核验官方能力的候选，正式权限、退市/PIT 完整性与再分发权
未通过前不会被标成可执行本地能力。设计与当期官方证据见
[跨市场数据契约与数据源矩阵](docs/design/cross-market-data-contracts.md)。

### 3.2 跨市场基准与可交易代理 ✅

优先接入覆盖面广、语义相对清楚的指数、收益率曲线和流动 ETF / 期货代理，使跨区域资产配置、相关性、
宏观敏感度和币种影响可以先被学习和复现。

范围目标：

- 中国内地、香港、美国股票市场代表性基准；
- 主要期限国债曲线或透明的债券指数/ETF 代理；
- 黄金、能源、工业金属、农产品的指数/ETF/期货代理；
- 本币与统一基准币两套收益，显式拆分资产收益和 FX 收益；
- 不同市场收盘时点下的可用信息对齐。

**完成定义**：固定跨市场样本可以通过时间序列和 Panel 研究、组合分析与风险报告；缺失日、时区和汇率
处理有 fixture、数据审计和帮助说明。此阶段不要求先支持境外真实交易。

已固定沪深 300、恒生和标普 500 三条不可交易价格指数基准，以及 `510300.SH`、`159920.SZ`、
`513500.SH` 三只中国场内 ETF 代理。指数按源市场 `tradeDate` 和中国研究 `availableDate` 分离；恒生与标普
同时提供本币和 CNY 指数水平，港币由 `USDCNH / USDHKD` 显式交叉推导。时间序列研究使用指数，Panel、
组合归因与风险报告使用 ETF 真实交易历史，不拼接指数和 ETF 曲线。已有中国国债/美国名义与实际国债曲线、
黄金及商品 ETF/期货代理继续进入同一研究底座。

bootstrap、每日维护、数据审计、Concept/Binding/Catalog、只读 SQL 与中英文帮助均已接入。真实回填覆盖
2005–2026 年三地指数和 2012–2026 年两条 FX；恒生本币/CNY 月收益 ResearchPlan 与三只 ETF Panel
均完成真实数据验收。固定样本、公式、异常语义和验收证据见
[跨市场基准与可交易代理](docs/design/cross-market-benchmarks.md)。港美个股仍严格属于 3.3。

### 3.3 香港与美国个股研究数据 ⬜

在 3.1/3.2 稳定后，再扩展个股横截面研究：证券主数据、价格/成交、公司行动、历史可投资状态、行业分类、
财务与估值 PIT。不能只抓当前仍上市股票，也不能把不同会计和字段定义硬映射成看似统一的值。

**完成定义**：港股和美股各选择一个明确 Universe，完成无幸存者偏差的历史样本、公司行动与财务可得时间
验证，并各用一个经典因子做探索/holdout 全链路。研究可先完成，交易引擎支持另行立项。

### 3.4 固收与商品专业数据深化 ⬜

境内曲线、商品期限结构、仓单和持仓已有第一版。后续由学习问题驱动补足信用、通胀、曲线、Carry、
Roll-down、连续合约和季节性等数据；跨区域扩展时保留原始单位、合约规则和信息可得时间。

**完成定义**：每新增一个数据域，都有字段目录、PIT/单位说明、质量审计、至少一个可复现研究和中英文帮助；
不以表数量或字段数量作为完成标准。

---

## 当前主线四：研究方法、组合与风险

### 4.1 统计方法覆盖审计与补全 🚧

不继续零散添加指标。先按横截面、时间序列、Panel、宏观状态和策略绩效审计现有推断方法，列清独立性假设、
重叠收益、自相关、小样本、多重检验和结构突变的处理边界。

优先候选包括：置换/随机化检验、适合问题的 bootstrap、walk-forward、滚动稳定性、协方差收缩和指标不确定性。
是否实施由具体学习练习触发，不能用不合适的收益重排制造虚假路径。

本项负责方法正确性和覆盖审计；通过审计且反复使用的方法可以沉淀为透明的 Markdown / Python 模板或经过测试的 helper，不再增加隐藏固定流程。

**完成定义**：形成方法矩阵与测试 fixture；每种正式报告说明为什么使用该估计或检验、假设不满足时怎样失败。

早期曾以固定双变量/多变量、分布比较和事件研究流程验证统计实现；该产品形态已于 2026-08-19 删除。HAC、
偏回归、VIF、滚动稳定性等方法仍可由 Research Python 使用 statsmodels / SciPy 实现，待真实重复需求出现后再
沉淀成透明模板与 fixture。横截面、Panel、宏观状态和策略绩效的审计与补全仍未完成，因此本项保持进行中。

### 4.2 经典研究复现计划 ⬜

按主题精选代表性研究，不批量复制 factor zoo：

- 股票：价值、质量、动量、反转、低风险、流动性；
- 跨资产：趋势、Carry、价值与防御；
- 债券：曲线、久期、Carry / Roll-down 与信用；
- 商品：趋势、期限结构、库存/仓单、持仓和季节性；
- 组合：分散化、再平衡、波动率目标和风险预算。

**完成定义**：每项复现都包含经济逻辑、原始定义、数据差异、本地化修改、探索/样本外、费后结果、反证与
结论；不因论文著名而降低准入标准。

### 4.3 多策略与组合构造研究 ⬜

把单因子、单策略和单资产的结果提升到组合层，先研究再产品化。至少比较等权、固定权重、波动率倒数、
风险预算和带约束的均值方差；协方差估计误差、换手、容量和再平衡成本必须同时呈现。

第一版优先使用冻结报告或策略收益序列做离线组合研究，不急于让多个策略共享真实账户和信号仲裁。

**完成定义**：同一组候选可以比较收益贡献、风险贡献、相关性、集中度、换手、压力情景和滚动稳定性；
系统不预设哪种优化器最好，等权始终作为基线。

### 4.4 绩效与回撤分析 ⬜

围绕长期目标补齐真正影响判断的指标与图表：

- 完整水下曲线与 Top 回撤的开始、谷底、恢复、深度和持续时间；
- Sharpe、Calmar、波动、尾部损失及其滚动表现和样本不确定性；
- 相对基准、分年/分状态表现、收益来源和成本占比；
- 单策略与组合的集中度、相关性、风险贡献和情景损失。

**完成定义**：结果页能回答“收益来自哪里、在哪些阶段成立、正常要承受多深多久的回撤、相对简单基线是否
值得额外复杂度”；不做无决策用途的指标军备竞赛。

### 4.5 可解释机器学习 💤

触发条件：线性/规则基线、数据 revision、walk-forward 和完整研究记录已经稳定，并出现明确的非线性研究问题。

届时模型输出仍作为普通因子或预测信号进入同一 holdout、费后和风险流程；冻结训练数据窗口、特征、参数和
模型版本，保留基线与可解释性。当前不做端到端价格预测，也不让自动搜索绕过实验计数。

---

## 长期阶段：模拟与实盘验证 💤

现有信号、模拟账户和实际成交回填继续维护。只有同时满足以下条件，才重新把实盘列为活跃主线：

1. 已形成少量而非海量候选策略/组合；
2. 假设、数据、代码、报告和版本可以完整重现；
3. 经预注册样本外、跨时期/状态和真实成本检验；
4. 收益来源、风险暴露、容量、最坏回撤和失效条件可以解释；
5. 数据维护、任务恢复、告警和备份在目标环境长期稳定；
6. 使用者理解策略而不是只接受一个指标或模型结论。

进入后依次为模拟运行、极小仓验证、执行偏差校准和有限自动化评估。券商接入、多账户、自动调仓均需独立
设计和授权；当前不预埋。

---

## 当前执行顺序

| 顺序 | 工作 | 状态 | 原因 |
|---|---|---|---|
| 1 | 1.5 M0–M3 响应式研究文档 | ✅ | Markdown / Python、Agent、运行与快照主闭环已完成 |
| 2 | 1.5 M4 Factor / Strategy 血缘交接 | ✅ | Python Factor / Strategy 草稿与来源快照交接已闭环 |
| 3 | 1.3 Research Curator 手动版 | ✅ | 已能发现并核验可沉淀的方法、数据和文档缺口 |
| 4 | 1.4 研究语义目录持续维护 | 🚧 | A 股截面与月末 Panel 已完成，后续仍由真实研究问题触发 |
| 5 | 3.1 跨市场数据契约与数据源矩阵 | ✅ | 中港美股债商扩展的共同前置，先防语义债 |
| 6 | 2.1 双语学习路径的第一批内容 | ⬜ | 公式、方法和概念解释本身就是产品能力 |
| 7 | 3.2 跨市场基准与可交易代理 | ✅ | 已用指数基准与境内 ETF 代理形成全球资产研究最小闭环 |
| 8 | 4.1 方法覆盖审计 + 4.2 首批经典复现 | ⬜ | 让工具建设始终挂在具体理论问题上 |
| 9 | 4.3 组合构造 + 4.4 绩效回撤 | ⬜ | 面向长期 Sharpe / Calmar 目标，但避免单指标拟合 |
| 10 | 3.3 个股、3.4 专业数据按学习问题推进 | ⬜ | 数据工程量大，必须由前置契约和研究需求拉动 |

同一时间原则上只激活一个重工程主线；帮助文章、方法笔记、数据审计与缺陷修复可以并行穿插。

## 当前明确不做

- 为展示“懂得多”而把每个知识点做成正式页面或数据库实体；
- 自动批量挖因子、自动揭示 holdout、自动上线策略；
- 当前阶段的一键转因子建议与自动创建因子草稿；
- 社区动态流、收益排行、跟单、营销和机构审批/合规工作流；
- 在当前阶段扩自动券商通道、多账户交易台或高可用实盘基础设施；
- 在缺乏可信数据和明确学习问题时产品化分钟/tick/L2 高频研究；
- 用单次回测的 Sharpe / Calmar、最高收益或最优参数决定研究成功。
