import type { zhDashboard } from '../zh/dashboard';

// English mirror of zhDashboard (structurally identical — enforced by typeof).
export const enDashboard: typeof zhDashboard = {
  placeholder:
    'Factor research and backtest panels are under construction — data pipeline and quote storage are ready.',
};
