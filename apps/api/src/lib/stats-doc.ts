// GENERATED FILE — do not edit. Source of truth: lib/stats.ts JSDoc.
// Regenerate: pnpm --filter api gen:stats-doc   (stats-doc.test.ts fails on drift)
export const STATS_DOC =
  "- stats.mean(xs: number[]) — Arithmetic mean (0 for an empty array).\n- stats.std(xs: number[], sample = true) — Standard deviation. sample=true uses the sample (n-1), false uses the population (n).\n- stats.pearson(xs: number[], ys: number[]) — Pearson correlation of two equal-length arrays (align/filter them first).\n- stats.spearman(xs: number[], ys: number[]) — Spearman rank correlation = Pearson on ranks. I.e. Rank IC.\n- stats.winsorize(xs: number[], p = 0.01) — Cross-sectional winsorization: clip values to the [p, 1-p] quantile range.\n- stats.quantileBuckets(values: number[], n: number) — Split elements into n buckets by ascending rank (returns each element's bucket 0..n-1). Bucket 0 = lowest factor value, n-1 = highest.\n- stats.navFromReturns(periodReturns: number[]) — Build a NAV series from per-period returns (starting at 1).\n- stats.annualizedReturn(periodReturns: number[], periodsPerYear: number) — Annualized return (geometric).\n- stats.sharpe(periodReturns: number[], periodsPerYear: number, rf = 0) — Annualized Sharpe (rf is the annualized risk-free rate).\n- stats.maxDrawdown(nav: number[]) — Max drawdown (negative, e.g. -0.32 means a max drop of 32%).\n- stats.median(xs: number[]) — Median (0 for an empty array).\n- stats.quantile(xs: number[], p: number) — The p-quantile (0 ≤ p ≤ 1) with linear interpolation; e.g. quantile(xs, 0.9) = 90th percentile.\n- stats.covariance(xs: number[], ys: number[]) — Sample covariance of two equal-length arrays.\n- stats.linearRegression(xs: number[], ys: number[]) — OLS linear regression y = slope·x + intercept; r2 is the coefficient of determination. Classic use: regress stock returns on index returns → slope is beta, intercept is (per-period) alpha.";
