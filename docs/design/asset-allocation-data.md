# 规划：大类资产配置数据（债券、商品与宏观）

> 2026-08-04 制定。目标是把现有股票 / ETF / 股指期货研究平台扩展为个人大类资产配置
> 研究平台。本文先确定研究问题、数据分层、PIT 口径和实施波次，不代表立即实现全部接口。
> 现有 ETF 交易语义以 [`etf-trading.md`](./etf-trading.md) 为准；通用数据扩展纪律以
> [`data-expansion.md`](./data-expansion.md) 为准；数据如何形成因子、发布预测信号并进入策略以
> [`factor-v2.md`](./factor-v2.md) 为准。

## 1. 背景与当前能力

平台已经具备：

- A 股宽基、风格和行业指数行情；
- ETF 全量元数据，以及首批 12 只主要 ETF 的日线和复权因子；
- 可搜索、看图、比较、回测和生成每日信号的 ETF 通道；
- CFFEX 股指期货实际合约、主力映射、结算参数和交易回测；
- 股票因子、市场状态、参数扫描和只读 SQL 分析。

首批 ETF 已包含黄金 `518880.SH`、5 年国债 `511010.SH` 和纳斯达克 100 QDII
`513100.SH`，因此平台已经可以做一个非常简化的股债金配置回测。当前缺口不是“能否交易
ETF”，而是缺少解释和区分债券、商品回报的底层数据：

- 债券：收益率曲线、资金利率、久期和信用利差；
- 商品：期货期限结构、展期收益、库存 / 仓单和持仓；
- 宏观：增长、通胀、信用 / 流动性和外部环境，且必须按实际发布日期使用。

本地 Tushare ETF 元数据快照中有 62 只债券型 ETF，其中 53 只处于上市状态；直接商品暴露
主要是 14 只黄金 / 上海金 ETF 和 3 只商品期货 ETF（豆粕、有色金属、能源化工）。名称含
“黄金”“有色”“商品”的产业股票 ETF 仍属于权益资产，不能归为直接商品暴露。

## 2. 目标与非目标

### 2.1 目标

1. 建立可交易的大类资产代理池，至少覆盖境内权益、海外权益、现金 / 短债、中长久期利率债、
   高等级信用、黄金和国内商品。
2. 支持资产收益、波动、回撤、相关性、风险贡献和滚动指标的统一比较。
3. 在统一策略 Lab 中支持等权、逆波动、风险平价、趋势过滤等低频配置方法，采用月度或季度调仓。
4. 对债券提供收益率曲线和久期解释，对商品提供期限结构和库存解释。
5. 建立 point-in-time（PIT）宏观数据语义，保证策略在任意日期只能看到当时已经发布的数据。
6. 让 Agent 能通过只读 SQL 回答“当前利率曲线是否陡峭”“黄金与实际利率关系如何”等问题，
   同时如实报告数据缺口和口径限制。

### 2.2 非目标

- 不在本期做分钟 / tick、ETF 套利、PCF 篮子或盘中 IOPV 策略；
- 不在本期研究单只信用债发行人、违约概率、主体评级迁移或逐券定价；
- 不把可转债当作普通债券；它是带权益凸性的混合资产，必须单列；
- 不在第一版接全球每个商品的供需数据库、天气、船运和地缘事件；
- 不在数据底座稳定前使用机器学习预测宏观或资产收益；
- 不在同一波次把商品 / 国债期货加入现有交易引擎。

## 3. 核心设计决策

### 3.1 可交易代理与底层驱动分层

同一资产保留两层数据，不能混为一张万能行情表：

1. **可交易层**：ETF 未复权行情用于成交，复权行情用于收益和持仓估值；反映真实费用、流动性、
   跟踪误差和上市时间。
2. **研究层**：收益率曲线、债券总回报指数、商品指数、期货曲线和宏观序列；用于长历史比较和
   回报归因，不直接假定可按指数点位成交。

ETF 回测回答“个人实际可以买到什么”，底层研究数据回答“该资产为什么涨跌”。当两者历史长度
不同，应在报告中同时展示代理资产起点和研究基准起点，不拼接成一条看似连续但不可审计的净值。

### 3.2 先研究、后交易期货

当前 `FutureContract` / `FutureDaily` 明确定义为 CFFEX 的 IF/IH/IC/IM，回测引擎还包含股指
期货专用的合约乘数、固定 tick 和保证金语义。商品和国债期货的交易所、报价单位、tick、乘数、
涨跌停、交割和移仓规则不同。

因此商品 / 国债期货第一阶段只进入研究侧；若未来确有交易需求，再单独设计通用期货资产模型和
引擎迁移，不偷偷放宽当前表的语义。

### 3.3 宏观数据必须按发布日期门控

宏观数据至少存在三个不同日期：

- `period`：数据描述的月份 / 季度；
- `releaseDate`：统计机构实际发布日期；
- `availableDate`：平台策略允许使用的最早日期，默认不早于 `releaseDate` 后的下一可交易时点。

回测按 `availableDate <= decisionDate` 做 as-of join，禁止按 `period` 直接向交易日填充。若源接口
没有历史修订版本，则保存 `retrievedAt` 并在报告中声明“当前终值回填，不具备完整 vintage”，
不能伪称完全复原了当时信息集。

### 3.4 需求拉动，不全量囤积

- ETF 元数据继续全量保存；日线只同步进入配置池或策略明确引用的代码。
- 收益率曲线只保存确定用于研究的曲线和期限点。
- 宏观只接能映射到增长、通胀、流动性或外部环境的系列。
- 商品先接综合指数和少量代表品种；没有研究问题的冷门合约不上。
- 每一类数据必须先定义要计算的指标和验收查询，再实现同步。

### 3.5 数据、因子与策略分层

本文只负责大类资产的数据底座、可交易代理和领域特征，不另建一套“债券研究 / 商品研究 / 配置研究”
回测产品。统一链路为：

```text
收益率曲线 / 期货曲线 / 库存 / 宏观观测
                    ↓
         Factor V2 数据字段与特征
                    ↓
  横截面 / 时间序列 / panel / 宏观状态检验
                    ↓
              不可变已发布 Factor
                    ↓
       策略 Lab 生成 ETF 目标权重并回测
```

- 描述性曲线、库存和宏观图表是发现假设的入口，不等于已经验证的 Factor；
- 因子有效性、holdout 和发布在 Factor V2 完成；
- 固定权重、逆波动、风险平价和趋势过滤属于策略或组合构建，在现有 Lab 使用 `period` 调仓；
- 收益贡献、风险贡献、相关性和情景表现作为 Lab 的配置结果分析；
- 商品 / 国债期货第一阶段只作为研究数据，实际交易仍映射到 ETF。

## 4. 目标资产池

### 4.1 分类口径

| 一级资产 | 二级暴露 | 可交易代理 | 研究基准 / 驱动 |
|---|---|---|---|
| 境内权益 | 宽基、红利、小盘、成长 | 现有主要股票 ETF | 指数行情、估值、市场状态 |
| 海外权益 | 美股等，以人民币投资者视角计价 | QDII ETF | 海外指数、USD/CNY |
| 现金 | 货币 ETF、短回购 | 场内货币 ETF | 回购、Shibor 短端 |
| 利率债 | 短、中、长、超长久期 | 国债 / 政金债 ETF | 国债收益率曲线、债券指数 |
| 信用债 | 高等级短融、公司债 | 高等级信用债 ETF | 信用利差、资金利率 |
| 混合债 | 可转债 | 转债 ETF | 转债指数、权益波动；与纯债分开 |
| 黄金 | 实物黄金 ETF | 黄金 / 上海金 ETF | SGE 现货、美国实际利率、汇率 |
| 商品 | 能源、工业金属、农产品 | 少量期货 ETF | 商品指数、各月期货、仓单 |

### 4.2 第一批候选代码

候选仅用于覆盖期限和风险来源，实施前还要比较上市时间、日均成交额、规模、费率、跟踪误差和
数据完整性；“同类规模最大”也不能替代这些检查。

| 暴露 | 候选 ETF | 当前状态 |
|---|---|---|
| 5 年国债 | `511010.SH` | 已同步 |
| 1–3 年国债 | `511160.SH` | 候选；历史较短 |
| 10 年国债 | `511260.SH` | 已同步，行情始于 2017-08-24 |
| 30 年国债 | `511090.SH` | 已同步，行情始于 2023-06-13 |
| 7–10 年政金债 | `511520.SH` | 候选 |
| 短融 | `511360.SH` | 候选；代表信用与现金增强 |
| 可转债 | `511380.SH` | 候选；单列混合资产 |
| 黄金 | `518880.SH` | 已同步 |
| 豆粕期货 | `159985.SZ` | 已同步，行情始于 2019-12-05 |
| 有色金属期货 | `159980.SZ` | 已同步，行情始于 2019-12-24 |
| 能源化工期货 | `159981.SZ` | 已同步，行情始于 2020-01-17；不能当作原油代理 |

若某类 ETF 的可用历史太短，研究报告使用底层指数说明长期风险特征，但正式可交易回测仍从 ETF
上市和行情可得日期开始。

2026-08-09 已完成三只商品期货 ETF 的首轮质量门禁：截至 2026-08-07 分别有 1,618、1,605、
1,588 条日线，上市以来与上交所开放日零缺口、零无成交，且日线与复权因子逐日 1:1。复权单日收益
绝对值均未超过 15%；2025 年以来 Tushare `amount` 字段的日均值约为 373,802、397,424、554,636
（接口单位为千元）。这些检查只证明 ETF 行情可以进入可交易研究，不代表已经取得底层期货 Carry、
库存或换月归因能力；能源化工 ETF 也继续按自身指数命名，不包装成原油代理。
空库仍由全量导入的 `major` 阶段同步；已有生产库由 bootstrap 检查上市首日、最新日线及逐日复权覆盖，
仅在三只 ETF 不完整时执行幂等回填，避免代码上线后出现可选但无行情的资产。

## 5. 数据清单与优先级

### 5.1 ETF 层

| Tushare 接口 | 内容 | 权限参考（2026-08-04） | 优先级 |
|---|---|---:|---|
| `etf_basic` / `fund_basic` | 分类、基准、上市状态、费率 | 已接入 | P0 |
| `fund_daily` | 未复权成交行情 | 5000 积分 | P0，已接入 |
| `fund_adj` | 分红复权因子 | 2000 积分 | P0，已接入 |
| `etf_share_size` | 份额、规模、NAV、收盘价 | 8000 积分 | P1 |
| `rt_etf_sz_iopv` | 深市实时 IOPV 和申赎 | 独立权限 | 暂不接 |

`etf_share_size` 可计算份额变化、估算净申赎、规模容量和收盘价相对 NAV 的折溢价，但不应把
“份额增加”直接解释为看多信号；做市、套利和产品迁移都会造成变化。

### 5.2 债券与利率层

| 数据 | Tushare 接口 / 来源 | 用途 | 优先级 |
|---|---|---|---|
| 财政部-中国国债收益率曲线 | 财政部发布、中债信息网承载的公开查询页 | 0.25/0.5/1/2/3/5/7/10/30 年、水平/斜率/曲率 | P0，已接入 |
| Tushare 中债国债收益率曲线 | `yc_cb`，独立权限 | 备选授权通道 | 当前 token 无权限，不阻塞官方源 |
| Shibor | `shibor` | 隔夜、1W、1M、3M 资金价格 | P0 |
| 交易所回购 | `repo_daily` | 个人可交易现金利率代理 | P1 |
| LPR | `shibor_lpr` | 政策传导背景 | P1 |
| 国债期货 | `fut_basic` / `fut_daily` / `fut_mapping` | 利率预期、基差；先研究只读 | P2 |
| 债券总回报指数 | 待确认指数商授权和 Tushare 覆盖 | 长历史、久期和信用比较 | P1，设权限门 |
| 中债国债 / 商业银行普通债 AAA / 中短票 AAA 曲线 | 中债官方历史下载 | 同日同期限信用利差 | P0，已接入 |
| 政金债曲线 | Tushare 当前公开目录不足 | 政金利差 | P2，外部源评估 |
| 银行间 DR007 / R007 | Tushare 当前公开目录未见 | 银行间流动性 | P2，外部源评估 |

`repo_daily` 是沪深交易所回购，不等于银行间 DR007 / R007，表名和 Agent 文档必须明确市场，
不能把两者合并成一个模糊的 `repoRate`。

### 5.3 商品层

| 数据 | Tushare 接口 | 用途 | 优先级 |
|---|---|---|---|
| 南华商品及品种指数 | `fut_index_daily` | 综合和分类商品收益代理 | P1 |
| 上海黄金现货 | `sge_basic` / `sge_daily` | 黄金现货、成交和持仓 | P1 |
| 各月期货合约与日线 | `fut_basic` / `fut_daily` | 期限结构、基差、展期收益 | P2 |
| 主力 / 连续映射 | `fut_mapping` | 连续观察和实际合约解释 | P2 |
| 仓单日报 | `fut_wsr` | 库存压力、供需代理 | P2 |
| 会员持仓排名 | `fut_holding` | 排名子集的多空与前五集中度 | P1，AU/CU/M 已接入，SC 缺源 |
| 全球原油、金属和农产品基本面 | EIA / LME / CFTC / USDA 等 | 全球供需深化 | P3，非 Tushare |

商品研究不能只用一条主力连续价格。第一版至少同时保存实际月合约，按同一交易日计算近远月价差；
连续收益必须记录换月规则和换月日，不能把合约切换造成的价差当作资产收益。

### 5.4 宏观层

第一版只保存下列能够形成明确解释变量的系列：

| 维度 | 系列 | Tushare 接口 | 频率 | 主要用途 |
|---|---|---|---|---|
| 增长 | 制造业 PMI、生产、新订单、原材料库存 | `cn_pmi` | 月 | 增长方向和库存周期 |
| 增长 | GDP 同比 | `cn_gdp` | 季 | 长期背景，不做短期择时主信号 |
| 通胀 | CPI 同比 / 环比 | `cn_cpi` | 月 | 居民通胀 |
| 通胀 | PPI 同比 | `cn_ppi` | 月 | 工业品价格和企业价格环境 |
| 信用 | 社融当月增量、存量 | `sf_month` | 月 | 信用扩张；派生同比和滚动和 |
| 货币 | M1 / M2 及同比 | `cn_m` | 月 | 货币活性和流动性背景 |
| 利率 | Shibor O/N、1W、1M、3M | `shibor` | 日 | 资金松紧 |
| 政策 | LPR 1Y / 5Y | `shibor_lpr` | 月 | 政策传导 |
| 外部 | 美国名义国债收益率 | `us_tycr` | 日 | 全球无风险利率 |
| 外部 | 美国实际国债收益率 | `us_trycr` | 日 | 黄金和成长资产驱动 |
| 外部 | USD/CNH | `fx_daily` | 日 | 人民币投资者的汇率暴露 |
| 日历 | 中国经济数据发布日期 | `cn_schedule` | 事件 | PIT 门控和发布审计 |

宏观序列不因为“常被市场讨论”就自动进入配置模型。每个系列必须声明原值、单位、频率、变换方法、
滞后和可得日期；派生同比、滚动和、z-score 均从原始值在本地可重复计算。

## 6. 建议数据模型

以下是实施方向，最终字段在对应波次开始前用真实 API 样本确认。

### 6.1 收益率曲线

`YieldCurvePoint`：

- `source`、`curveCode`、`curveName`；
- `tradeDate`、`availableDate`；
- `curveType`（到期 / 即期）；
- `termYears`；
- `yieldPct`；
- `retrievedAt`；
- 主键建议为 `[source, curveCode, curveType, tradeDate, termYears]`。

保留源曲线的全部真实期限点；常用 1/2/5/10/30 年由查询或派生层选择，不在同步时用插值覆盖原值。
财政部曲线约在工作日 17:30 发布，因此 `availableDate` 固定为 `tradeDate` 后的下一上交所交易日；
研究和策略一律按 `availableDate <= decisionDate` 做 as-of join。

### 6.2 宏观长表

宏观系列数量会逐步增加，建议使用显式系列目录加窄长表，而不是每个接口复制一张宽表：

- `MacroSeries`：`seriesKey`、中英文规范名、频率、单位、来源、源接口、默认变换；
- `MacroObservation`：`seriesKey`、`period`、`value`、`releaseDate`、`availableDate`、
  `vintageDate`、`retrievedAt`；
- 主键建议为 `[seriesKey, period, vintageDate]`；没有 vintage 时使用固定的首次抓取版本标识，
  后续值变化追加版本，不静默覆盖。

`MacroObservation` 只存数值时间序列；发布日历原始事件可另存 `MacroReleaseSchedule`，避免把计划
发布日期和实际观测值混成同一事实。

### 6.3 ETF 份额规模

`EtfShareSize`：`tsCode`、`tradeDate`、`totalShare`（万份）、`totalSize`（万元）、`nav`、
`close`、`exchange`，主键 `[tsCode, tradeDate]`。Agent 文档必须写清单位；净申赎是派生估计值，
不作为源字段入库。

### 6.4 商品研究表

第一步分别保存 `CommodityIndexDaily` 和 `SgeDaily`，避免为了少量数据立即抽象万能行情表。进入
合约曲线波次前，再决定新建研究专用商品期货表，还是经过完整引擎回归后泛化现有期货表；禁止在
没有交易语义设计和回归测试的情况下直接修改 `FutureContract` 的“股指期货”定义。

## 7. 研究指标与方法

### 7.1 统一资产指标

- 1/3/6/12 个月收益和趋势；
- 20/60/120/250 日波动率；
- 最大回撤、回撤持续时间和恢复时间；
- 滚动相关性和下行相关性；
- 风险贡献、换手率、交易成本和配置漂移；
- ETF 日均成交额、规模、上市历史和缺失率。

### 7.2 债券指标

- 曲线水平：10 年收益率或曲线主成分；
- 斜率：`10Y - 1Y`、`10Y - 2Y`；
- 曲率：中端相对长短端的曲率指标；
- 收益率变动：1/5/20/60 日 bp 变化；
- 久期压力测试：收益率平移 ±50 / ±100bp 的近似价格影响；
- carry / roll-down：只有取得可靠债券指数或足够曲线口径后再实现；
- 信用利差：中债中短票 AAA 或商业银行普通债 AAA 减中债国债，严格限定同日、
  同期限并乘 100 转为 bp；不插值，不用 ETF 收益差冒充静态信用利差。

### 7.3 商品指标

- 1/3/6/12 个月动量和波动；
- 近月—次近月、近月—远月年化价差；
- backwardation / contango 状态；
- 明确换月规则后的展期收益；
- 仓单总量及 5/20/60 日变化；
- 成交量、持仓量和流动性；
- 黄金相对美国实际利率、USD/CNH 的滚动关系。

### 7.4 宏观状态

第一版采用透明、连续的状态分数，不先做脆弱的四象限硬分类：

- 增长分数：PMI、新订单及其变化；
- 通胀分数：CPI、PPI 及其变化；
- 流动性分数：Shibor、曲线短端、M1 / M2 和信用脉冲；
- 外部分数：美国实际利率、名义利率和 USD/CNH。

所有滚动标准化只使用截至当日已发布的历史，窗口不足时返回空值。离散“复苏 / 过热 / 滞胀 /
衰退”可以作为显示层解释，但不得成为无法审计的手工标签。

### 7.5 从特征到因子与交易代理

第一批研究模板按 Factor V2 的协议落地，不能只停留在描述性看图：

| 数据 / 特征 | 候选因子假设 | 评估协议 | 预测目标 | 第一阶段交易代理 |
|---|---|---|---|---|
| ETF 20/60/120 日收益 | 趋势具有持续性 | time-series | 各 ETF 未来收益 | 原 ETF |
| 国债收益率 20/60 日变化 | 收益率下行对应债券价格走强 | time-series | 不同久期债券 ETF 未来收益 | 5Y/10Y/30Y 国债 ETF |
| 曲线斜率及变化 | 曲线状态影响不同久期的相对收益 | time-series / panel | 久期 ETF 相对收益 | 国债 ETF 组合 |
| 近远月年化价差 | backwardation / Carry 对未来收益有解释力 | time-series / panel | 单品种自身 / 商品间相对收益 | 商品期货 ETF |
| 商品自身动量 | 单品种趋势具有持续性 | time-series | 商品 ETF 未来收益 | 商品期货 ETF |
| 库存 / 仓单变化 | 供需压力影响对应商品收益 | time-series / panel | 商品未来收益 | 商品期货 ETF |
| 增长、通胀、流动性状态 | 不同宏观状态对应不同资产条件收益 | macro-regime | 多资产 ETF 条件收益 | ETF 配置池 |
| 美国实际利率、USD/CNH | 实际利率和汇率影响人民币黄金收益 | time-series | 黄金 ETF 未来收益 | 黄金 ETF |

每个模板必须冻结输入字段、变换、方向、horizon、目标和可得日期。研究通过后发布并锁定 Factor，
策略只引用唯一 `Factor.key`；底层字段或公式本身不能直接成为交易信号。

### 7.6 策略 Lab 配置基线

按月调仓，先比较以下可解释基线：

1. 固定权重；
2. 等权；
3. 逆波动；
4. 约束风险平价；
5. 固定权重或风险平价叠加 10 / 12 月趋势过滤。

报告必须同时给出收益、波动、最大回撤、换手和成本；不能只按夏普比率选方案。宏观状态第一阶段
只用于分组复盘和压力解释，验证稳定后才允许影响实时权重。

## 8. 实施波次

### 波次 0：权限与样本探针

不改 schema，使用现有 Tushare token 对候选接口逐个做小样本验证：

- 是否有权限、频率和单次行数限制；
- 最早 / 最新日期、更新时间和历史缺口；
- 字段单位、空值、重复键和修订行为；
- `yc_cb`、`etf_share_size` 等独立 / 高积分接口是否实际可用；
- 目标 ETF 的上市历史、近一年成交额和行情 / 复权覆盖。

输出一份机器可重复执行的探针和结果摘要。接口文档权限会变化，实施以真实调用为准。

**2026-08-06 实施状态：**已新增只读命令
`pnpm --filter api probe:asset-allocation -- --date YYYYMMDD [--json]`，不会写数据库，并将正常、空结果、
权限不足、参数错误和网络错误分别记录。以 `20260731` 为样本日的真实调用结果为：21 个接口有样本、
1 个接口为空、1 个接口无权限、0 个参数错误、0 个网络错误。

- `etf_share_size`、Shibor、交易所回购、LPR 可用；
- `yc_cb` 返回 `40203`，当前 token 无中债国债收益率曲线权限；后续已按预案评估并接入财政部发布、
  中债信息网承载的官方公开曲线查询页，不把网页价格或债券 ETF 反推值冒充曲线；
- 南华商品指数、上海黄金现货、商品期货合约 / 日线 / 主力映射 / 仓单 / 持仓均可用；
- PMI、GDP、CPI、PPI、社融、货币供应量、美国名义 / 实际国债收益率和中国数据发布日程可用；
- `USDCNH.FXCM` 在指定样本日返回空结果，说明调用未报权限错误，但不能据此认定当前更新正常，接入前
  还需单独检查最新水位和历史连续性。

本次只是权限与响应结构探针，不等同于已经通过字段单位、重复键、修订和 PIT 验收。后续仍按波次逐类
落库，不能因为接口可调用就一次性全量同步。

### 波次 1：可交易资产池

- 从候选代码中按流动性、历史和费率确定每类一个主要代理；
- 扩充 `MAJOR_ETF_CODES` 并按“代码 × 年份”回填行情和复权；
- 给配置池增加明确的一级 / 二级资产分类，产业股票商品主题不得标为直接商品；
- 补齐 Agent 搜索、SQL 说明和 ETF 比较验收；
- 不改现有 ETF 成交、费率和 T+0 / T+1 语义。

### 波次 2：国债曲线与资金利率

- 落库 `YieldCurvePoint` 和 Shibor；
- 计算固定期限、斜率、曲率和 bp 变化；
- 接入只读 SQL、数据审计和日常维护；
- 用 5 / 10 / 30 年国债 ETF 验证收益率变化与价格方向；
- 若 `yc_cb` 权限不可得，停止曲线实现并评估官方 / 授权替代源，不用网页抓取悄悄替代。

**2026-08-07 国债曲线纵向切片：**数据来自财政部[中国国债收益率曲线查询页](https://yield.chinabond.com.cn/cbweb-czb-web/czb/moreInfo?locale=cn_ZH&nameType=1)，
口径依据其[编制说明](https://indices.chinabond.com.cn/cbweb-czb-web/czb/bzcxsmDown?locale=)。已实现 `YieldCurvePoint`、官方曲线客户端和
`pnpm --filter api sync:rates [start] [end]`。完整同步 2006-03-01 至 2026-08-06 共 45,999 个点，
9 个期限各 5,111 条；无重复，且没有 `availableDate <= tradeDate` 的记录。日期按 `Asia/Shanghai`
解析，避免官方“北京时间零点”时间戳被 UTC 误读为前一天。只读 SQL 已登记字段、收益率百分比单位和
PIT 口径。

Factor V2 已开放 2Y/5Y/10Y/30Y 国债收益率输入，内置 10Y 收益率 20 日下行、10Y−2Y 斜率和
2Y/5Y/10Y 曲率三个债券模板；曲线模板只允许债券型 ETF 作为预测目标。5Y/10Y/30Y 国债 ETF 的真实
20 日前瞻报告得到 4,853 条观测，研究与策略引擎都只读取决策日已经可得的曲线点。尚未完成的波次 2
内容是收益率曲线更细的生产告警；Shibor 已在 Phase 5 国内流动性数据切片中完成。

### 波次 3：PIT 宏观底座

- 落库系列目录、宏观观测和发布日历；
- 先接 PMI、CPI、PPI、M1 / M2、社融；
- 再接美国名义 / 实际利率与 USD/CNH；
- 所有回测读取统一走 `availableDate` 门控；
- 增加未来函数测试：月度数据在发布日期之前必须不可见；
- 报告披露没有完整 vintage 的系列。

**2026-08-09 首批纵向切片：**已落库 `MacroSeries`、`MacroObservation` 和
`MacroReleaseSchedule`，接入制造业 PMI、CPI 同比、PPI 同比，并加入 weekly maintenance、bootstrap
历史补全、只读 SQL 说明和数据质量审计。真实同步覆盖 2005-01 至 2026-07，共保存 775 个观测 vintage；
相同区间幂等重跑新增 0 条。2026 年起使用 `cn_schedule` 的官方发布日期，并映射到首个上交所开市日；
该日历没有更早历史，因此旧数据不伪造发布日期，而分别采用月末后 7 日（PMI）和 20 日（CPI/PPI）
的保守可得日，并标记为 `conservative_lag`。

Tushare 宏观接口返回的是当前值，不提供完整历史修订快照。首次回填的旧观测因此标记为
`latest_value_backfill`，只可用于明确披露最终值偏差的探索研究，不能冒充当时真实 vintage；持续同步会
从当前时点开始积累 `captured_as_available` vintage。下一步先实现统一 as-of 查询与未来函数测试，再接
M1/M2 和社融，不能让后两者阻塞已经可验证的增长、通胀最小闭环。

**2026-08-11 国内流动性与信用数据切片：**Tushare 真实权限探测确认 `cn_m`、`sf_month` 和
[`shibor`](https://tushare.pro/document/1?doc_id=149) 均可用。规范目录由 3 条扩为 13 条：新增 M1/M2
余额及同比、社融当月增量与存量，以及 Shibor O/N、1W、1M、3M。余额单位分别固定为亿元和万亿元，
利率固定为百分比；`MacroObservation.period` 明确支持月频 `YYYYMM` 与日频 `YYYYMMDD`，不另建一张
只为 Shibor 服务的宽表。

真实全量同步 2005-01 至 2026-07 共 21,919 个 observation vintage：截至 2026-06，四条 M1/M2
序列各 258 条，社融增量 258 条、存量 140 条；四条 Shibor 自 2006-10-08 起各 4,928 条。Shibor 接口
单次最多 2,000 行，同步按自然年分段，避免长区间静默截断；同区间幂等复跑新增 0 条。Shibor 官方在
交易日 11:00 发布，
因此当日收盘研究可用当日值，非上交所开市日则映射到下一开市日，并标记 `published_intraday`。当前
`cn_schedule` 未提供 `cn_m` / `sf_month` 发布事件，二者不伪造官方发布日期，继续采用月末后 20 日的
保守可得日。bootstrap、weekly maintenance、宏观 PIT 审计和 Agent 只读 SQL 已同步纳入全部 13 条系列。
全库审计得到 0 个无效可得日、0 个非交易日可得记录和 0 条缺失必需系列；21,807 条历史最终值回填仍
只允许探索。这批数据只扩充底座；五轴宏观状态模型仍在 Phase 5 后续切片实现。

**2026-08-11 外部利率与汇率数据切片：**Tushare `us_tycr`、`us_trycr` 与 `fx_daily` 真实样本均通过。
美国名义/实际曲线复用 `YieldCurvePoint`，curve code 分别为 `us_treasury_nominal` 和
`us_treasury_real`；只保存源接口真实给出的期限点，不对缺失期限插值。USD/CNH 新增 `FxDaily`，原样保存
FXCM bid/ask OHLC 与报价笔数，研究收盘价以后再确定性计算 `(bidClose + askClose) / 2`，不把中间价
覆盖回原始行情。

2005-01-01 至 2026-07-30 的真实全量回填得到 61,021 个名义曲线点、25,690 个实际曲线点和 4,382 根
USD/CNH 日线；两条 10 年曲线都从 2005-01-03 起覆盖，USD/CNH 从 2012-02-18 起覆盖。三个接口均按
自然年请求，分别低于单次 2,000 / 1,000 行上限；美国假日的全空名义曲线占位行不落库，也不前值填充。
`tradeDate` 保留美国或 GMT 源日期，`availableDate` 一律使用严格晚于源日期的首个上交所交易日，因为
中国当日收盘时美国同日曲线和完整全球 FX 日线尚未形成。bootstrap 覆盖门、daily maintenance、PIT
审计、能力探针和 Agent 只读 SQL 已同步接入；这三条市场序列采用 `not_revised` 风险血缘，不与宏观
`latest_value_backfill` vintage 混用。全库审计对两条 10 年曲线和 USD/CNH 共检查 15,175 个日频观测，
结果为 0 个无效可得日、0 个非交易日可得记录、0 个无效收益率或倒挂收盘报价。

**2026-08-11 中债信用曲线数据切片：**Tushare `yc_cb` 的当前文档只列出国债曲线，且当前
token 缺少独立权限，无法构成同口径信用利差。按数据源门槛改用中债官网
[PBC 公开历史查询页](https://yield.chinabond.com.cn/cbweb-pbc-web/pbc/showHistory?locale=zh_CN)提供的
正式历史 XLSX 下载接口，同时保存中债国债、中债商业银行普通债 AAA 和中债中短期票 AAA
三条原始到期收益率曲线。这不是 HTML 抓取；客户端会校验 XLSX schema、日期范围、重复点和收益率
边界，源端缺少的期限保持缺失，不插值、不前值填充。

真实回填 2006-01-01 至 2026-07-30 共落库 106,550 个原始曲线点，幂等复跑计数不变。5Y
国债从 2006-03-01 起覆盖 5,106 日，5Y 中短票 AAA 从 2008-01-02 起覆盖 4,643 日，5Y
商业银行 AAA 从 2009-12-24 起覆盖 4,148 日。三条 5Y 曲线共审计 13,897 个观测，
0 个无效可得日、0 个非交易日可得记录、0 个无效收益率。中债曲线在中国收盘后发布，
`availableDate` 因此是源日期后首个上交所交易日。`sync:credit-curves`、bootstrap、daily
maintenance、质量审计和 Agent SQL 已全部接入。当前仅用于平台内部研究；原始数据再分发、
商业化基准或衍生产品在上线前仍须按中债授权要求单独复核。

**2026-08-11 商品会员持仓排名数据切片：**Tushare
[每日成交持仓排名 `fut_holding`](https://tushare.pro/document/2?doc_id=139) 的真实权限和历史均可用，
但数据是实际合约的成交、持买仓和持卖仓三个排名子集的并集，不是全市场仓位，也没有商业/
投机交易者分类。`CommodityHoldingPosition` 每日先在本地实际月合约中按未平仓量选一个代表合约
（成交量和代码只做并列破平），再保存排名成交量、排名多/空持仓、前五持仓、各榜人数、合约 OI
分母和完整来源血缘。后续可确定性派生排名净头寸、排名持仓/OI 和前五/前二十集中度，
但报告标题必须保留“排名会员”限定。

真实回填 2015-01-05 至 2026-07-30 共得到 7,745 个品种日：AU/CU 各 2,590 日，覆盖率
均为 99.88%；M 为 2,565 日，覆盖率 98.92%。34 个源端空白日保持缺失，不前值填充；
全量幂等复跑计数不变。早期交易所额外提供的“期货公司/非期货公司”类别汇总会与会员排名
重复，因此精确剔除并用 `excludedSummaryRowCount` 记录，全库共 4,136 行。M2105 在
2020-11-06 的六个非空字段全部恰为前后日口径的两倍，全历史扫描仅此一日；系统按精确清单
除 2 并保存 `sourceCorrectionApplied=true`，不做泛化异常裁剪。Tushare 文档称 SHFE 包含 INE，
但 SC 在 2020/2023/2025/2026 的实际合约与交易所探测都返回空，所以当前只开放 AU/CU/M，
不用原油 ETF、仓单或其他品种持仓冒充 SC 会员排名。全量审计为 PASS，无效行 0，三品种最新日
均无滞后；bootstrap、daily maintenance、Agent SQL 和数据审计已纳入。

**2026-08-09 as-of 查询契约：**宏观读取已统一为两种不可混淆的 revision policy。严格研究使用
`as_available`，同时要求 `availableDate <= decisionDate` 和 `vintageDate <= decisionDate`，再为每个
系列与观察期选择当时最新的本地 vintage；因此在 2026-08-09 首次捕获之前，不会凭空产生“实时历史”。
`latest_vintage` 只用于探索，会按历史可得日对齐当前数据库里的最新 revision，并在结果中强制统计
`futureVintageRows` 与 `latestValueBackfillRows`。带有未来 vintage 的探索结果不得升级为 validated 或
production Factor。未来函数 fixture 已覆盖发布前不可见、决策日后修订不可见和 data cutoff 三条边界。

首个宏观状态特征已在该契约上实现：PMI 构成增长轴，CPI/PPI 构成通胀轴，每条系列以水平和 3 个月
变化形成滚动 z-score，使用 60 个月窗口、至少 24 个月历史，并将单项限制在 ±3。真实最终值探索历史
可形成 235 个月末状态点（2007-01 至 2026-07）；严格 PIT 历史在 2026-08-09 本地开始积累 vintage，
不会为了做出漂亮长回测而回填伪造的实时状态。状态分数仍只是 Macro Regime 研究输入，尚未映射为
资产收益结论或策略权重。

Macro Regime Report 的后端样本与统计契约已经落地：月末状态只连接决策日之后固定交易日 horizon 的 ETF
后复权收益，按状态展示条件收益分布、波动、正收益比例、Newey-West t 值、阶段持续时间和切换频率，
并增加前一期状态分组作为滞后敏感性对照。真实四资产探索样本得到 78 个状态月和 310 个资产观察；由于
使用最终值历史，772 条未来 revision 被明确披露，不能进入发布门。

统一 Factor worker/API 已接入首个冻结模型 `china_growth_inflation_regime_v1`。API 自动冻结数据截止日，
报告详情以 `macro_regime` 分型返回；真实 API/worker 验收复现了上述 78/310 样本。发布门要求研究协议使用
`as_available`，且报告同时证明无未来 vintage；最终值探索报告只能用于发现假设。

Factor 研究工作台现已提供该模型的只读入口、条件收益 ETF / horizon / revision policy 参数和四象限报告。
真实浏览器从界面完成的一次 2015-01 至 2025-01 运行得到 121 个有效月份、425 条资产观察和 40 次切换，
并将 720 条未来 revision 标为“仅探索”，不提供发布或策略引用动作。验收截图见
[`apps/web/acceptance/macro-regime-report.png`](../../apps/web/acceptance/macro-regime-report.png)。验收还纠正了
多源截止日的口径：宏观捕获日期与 ETF 市场日期异步，快照截止取已观察源日期的最大值，再由宏观和行情
loader 各自执行 PIT、horizon 与缺失数据约束，不能简单取“共同最早日期”裁剪宏观 vintage。

当前仍不开放自定义宏观模型或宏观状态到策略权重的映射；状态研究负责发现条件收益假设，仓位映射仍由
策略回测验证。宏观 holdout 则等待本地积累出足够的真实 `as_available` vintage 后再开放。

### 波次 4：商品研究底座

- 先接南华商品指数和上海黄金现货；
- 选择能源、金属、农产品各 1–2 个代表品种做合约级探针；
- 保存实际月合约，计算期限结构和明确规则的连续收益；
- 再接仓单；会员持仓排名在证明有增量研究价值后才接；
- 保持研究只读，不开放商品期货策略下单和回测成交。

**2026-08-10 首批实际合约与 Carry V1：**已固定 AU 黄金、CU 铜、SC 原油、M 豆粕四个代表品种，新增
独立 `sync:commodity-futures` 命令，将商品实际交割月合约和原始日线写入通用 FutureContract / FutureDaily
表，同时把股指元数据刷新从“全表替换”改为按 IF/IH/IC/IM 隔离替换，防止两类数据互删。商品合约仍不
进入交易引擎。真实接口显示四品种的 multiplier 均为空，因此数据库按原值可空保存；研究 Carry 不伪造
乘数，也不依赖名义金额换算。

Carry 每日从距离交割至少 10 天、成交量和持仓量均为正的实际合约中选择最近两个不同交割月，以
`ln(近月结算价 / 远月结算价) × 365 / 交割日间隔` 年化；正值为 backwardation，负值为 contango。
所有输出保留近远月代码、结算价、交割日、期限间隔和换月标记，不构造未经定义的“连续价格收益”。
结算价只能在期货收盘后确定，因此特征 `availableDate` 固定为 `tradeDate` 后的下一上交所交易日；
横截面和时间序列研究都按该日期做 as-of join，日历缺失时失败，不允许同日 ETF 收盘收益使用当日结算价。
2026-07-01 至 2026-08-07 的真实验收保存 934 个历史合约元数据和 1,344 条窗口日线，AU/CU/SC/M 各形成
28 个曲线点并各发生一次近月切换。

**2026-08-10 商品横截面 Panel V1：**`annualizedLogCarry` 已登记为 Factor V2 商品字段，受控模板把
AU/CU/SC/M 的当时可得期限结构分别连接到黄金、有色、能化和豆粕 ETF 的未来收益。共同月末最多容忍
7 个自然日的期货特征陈旧度，ETF 目标仍按真实交易日后复权价格计算；有色、能化只是铜和原油的类别
代理，报告明确披露非一一对应的基差风险。API 冻结四个期货品种与四个 ETF
均已落库的截止日。产品只显示四资产商品池并复用 Panel 排序报告，不提供 `strategyKey`、发布、组合或
Lab 入口。同一 Carry 的单品种时间序列检验见下段；两类证据都不能跳过样本外与策略成本验证直接进入
ETF 配置策略。

真实长历史同步覆盖 2015-01 至 2025-07 的 469 个重叠合约和 107,385 条日线。工作台真实运行到
2025-01-27 得到 294 条观测、59 个有效共同月份；平均 Rank IC 为 0.019、年化 ICIR 为 0.13，单边
10bp 后的多空年化为 5.10%。弱排序相关与正的样本内组合结果并不一致，当前证据不支持跳过冻结 holdout
和策略验证直接交易。验收脚本与截图分别为
`apps/web/e2e/commodity-carry-panel.mjs` 和
[`apps/web/acceptance/commodity-carry-panel-report.png`](../../apps/web/acceptance/commodity-carry-panel-report.png)。

**2026-08-10 商品 Carry 时间序列 V1：**受控模板复用同一实际合约 Carry，但按 AU、CU、SC、M 分别
连接到黄金、有色、能化、豆粕代理 ETF，逐资产检验自身信号与未来 20 个交易日收益，不再做共同月末的
横向排名。期货特征最多向 ETF 交易日对齐 7 个自然日，特征可得日不得晚于决策日，目标日不得越过冻结
截止；API 截止同时受所选 ETF 与对应期货品种的数据水位约束。自定义定义、发布和策略消费继续拒绝该
研究专用输入。

真实工作台从 2015-01-01 运行到 2025-01-27，得到 2,428 个有效交易日、6,069 条观测。黄金、豆粕、
有色代理的 Carry/未来收益相关性为 0.079、0.109、0.129，能源化工代理为 -0.113；对应 Newey-West
t 为 2.28、1.48、1.56、-2.00。黄金与能源化工的单项统计量越过常用双侧 5% 阈值，但方向不一致，
且仍受多重检验、样本内探索和类别代理误差影响。当前证据提示商品间存在明显异质性，尚不支持把“正
Carry”设为统一 ETF 配置规则；下一步应先做冻结 holdout 或更严格的代理/连续收益口径，
而不是开放商品期货交易。验收脚本为 `apps/web/e2e/commodity-carry-time-series.mjs`，截图见
[`apps/web/acceptance/commodity-carry-time-series-report.png`](../../apps/web/acceptance/commodity-carry-time-series-report.png)。

**2026-08-10 商品仓单数据层 V1：**已接入 Tushare [仓单日报 `fut_wsr`](https://tushare.pro/document/2?doc_id=140)，
新增 `CommodityWarehouseReceipt` 与独立 `sync:commodity-warehouse-receipts` 命令，并由 bootstrap 在表为空或
历史/近期覆盖不足时回填 2015 年以来数据。同步按“品种 × 月”分页，达到 1,000 行上限时继续用 offset；
若上游忽略 offset、重复整页或返回范围外日期则失败，不接受静默截断。

V1 按精确品种名隔离 AU=黄金、CU=铜、SC=中质含硫原油、M=豆粕，再剔除仓库名包含“合计/总计/小计”
的汇总行。该规则解决了两个真实接口陷阱：查询 CU 会同时返回 `铜(BC)`；SC 则会在同一天的不同物理仓库
同时使用“桶”和“吨”。由于缺少各批原油密度/品级，吨不能可靠换算成桶，表的主键因此是“品种 × 日期
× 单位”，两种 SC 序列分开保存，绝不相加。AU 千克、CU 吨、M 手保持单一单位；每日保存上游原始单位、
聚合单位、总量、完整时才汇总的 `volumeChange`、来源行数和采集时间。仓单日报在收盘后形成，
`availableDate` 与 Carry 一样固定为下一上交所交易日。

完整真实回填覆盖 2015-01-05 至 2026-08-07，共落库 11,158 条单位分层日聚合：AU 2,796、CU 2,816、
M 2,697；SC 为 1,868 条桶序列和 981 条吨序列。历史扫描还识别出 AU 17 个把千克错标成吨的确定日期；
代码只对这 17 日做精确修正，同时保存 `sourceUnit=吨` 和 `unitCorrectionApplied=true`，不使用宽泛的
自动猜测。全量数据没有 `availableDate <= tradeDate`、非上交所开市可得日或缺失原始单位。

**2026-08-10 仓单生产维护与质量门：**daily maintenance 已接入最近 20 个上交所交易日的
滚动刷新，既覆盖正常行情发布，也覆盖“没有行情缺口、只重试信号”的运行路径，用于吸收交易所近期修订。
批量 repair 不会每修一天重复请求仓单，而在整个修复结束后只刷新一次最近窗口。同步后必须通过品种名、
单位、来源行数、报告日、下一交易日可得日和 AU 精确单位修正的结构审计；品种最新报告落后 1–3 个交易日记为
warning，超过 3 日则阻断该次维护。`audit:data` 和 weekly maintenance 同时输出独立仓单 finding。

真实增量验证以 2026-08-07 为截止，重拉 2026-07-13 以来 20 个交易日，入库并审计 77 条聚合：
AU/CU/SC 各 19 条，M 20 条；四个品种最新日均为 2026-08-07，可得日为 2026-08-10，落后、PIT 和单位异常均为 0。

**2026-08-10 仓单时间序列 Factor 观测 V1：**Factor V2 已登记研究专用原始字段
`commodity.warehouseReceipt.volume`。装载器只负责按 `availableDate <= decisionDate` 进行 point-in-time
对齐、最多沿用 7 个自然日并连接 AU/CU/M 各自的代理 ETF；特征变换明确留在 Factor 代码中，首个冻结定义为
`pressure20(t) = log1p(W_asof(t-20 个 ETF 交易观测)) - log1p(W_asof(t))`。正值表示仓单下降、潜在供给压力
上升。SC 因吨/桶不可审计合并而 fail closed，不进入这一定义。

该字段目前仅供受控研究任务使用：自定义 Factor、发布、策略 Lab、每日信号均拒绝消费，避免尚未验证的
原始序列绕过研究门进入交易。探索样本固定为 2015-01-05 至 2025-01-27；AU、CU、M 分别得到
2,408/1,196/1,209 条观测，`pressure20` 与未来 20 个交易日 ETF 收益的相关性为 0.017/-0.125/0.049，
Newey-West t 为 0.25/-1.44/1.08。证据弱且方向不一致，当前不支持交易化，也不因样本内结果修改公式；
2025-01-28 起的 holdout 尚未打开。

**2026-08-11 仓单研究产品收口：**通用 Factor 模板元数据已经显式声明允许和默认资产，AU/CU/M 默认选中；
SC 在前端保留可见的不可用解释，并由 API 硬拒绝。共同 `dataCutoff` 同时受所选 ETF 最新行情和对应仓单
`availableDate` 约束，报告展示仓单 point-in-time 口径且不提供发布入口。真实浏览器得到 2,408 个有效时期、
4,813 条观测，与上述冻结结果一致；验收脚本为
`apps/web/e2e/commodity-warehouse-receipt-time-series.mjs`，截图见
[`apps/web/acceptance/commodity-warehouse-receipt-config.png`](../../apps/web/acceptance/commodity-warehouse-receipt-config.png)
和
[`apps/web/acceptance/commodity-warehouse-receipt-report.png`](../../apps/web/acceptance/commodity-warehouse-receipt-report.png)。
5/60 日变化、标准分与仓单 panel 均继续后置到 Phase 5 讨论，不作为 Factor V2 / Multi-Asset V1 收口条件。

### 波次 5：Factor V2 与策略 Lab 闭环

- 把已落库曲线、商品和宏观字段登记到 Factor V2 数据字段目录，生成 Monaco、Agent 和批量预载元数据；
- 按第 7.5 节实现时间序列、panel 和宏观状态研究模板，不新建独立配置研究回测页；
- 研究通过后发布并锁定 Factor，策略 Lab 显式引用唯一 `Factor.key`；
- 在策略 Lab 提供固定权重、等权、逆波动、风险平价和趋势过滤对照，继续使用 `period` 调仓；
- 在 Lab 结果中统一展示资产收益、风险、相关性、收益贡献、风险贡献、配置漂移和情景分析；
- Agent 能解释权重变化所使用的确定性数据，不把宏观相关性表述成因果结论；
- 经过样本外和成本验收后，再讨论宏观状态是否进入策略权重。

截至 2026-08-09，Lab 已完成其中的收益贡献、风险贡献、交易成本、配置漂移、资产类别滚动相关和
利率环境分解 V1：
核算由回测引擎输出，按批准 Panel 研究池聚合资产类别，并与组合权益变化强制对账。相关性只使用相邻
交易日均有真实复权行情的市场收益，提供 60/120 日窗口、逐格有效样本数、最新热力图和月末滚动类别对；
前端不从成交或持仓反推。利率环境只使用决策日已可得的官方国债曲线，以 10 年收益率 60 观察期方向和
10Y−2Y 利差相对 252 观察期中位数形成透明的四状态条件表现，不参与当次策略权重。完整增长、通胀、
信用和流动性阶段及情景分析仍须等待宏观 PIT/vintage 底座；它们不得阻塞已完成的 ETF Panel 研究、
策略回测、部署和每日信号闭环。

## 9. 验收标准

### 9.1 数据正确性

- 每张新表有真实源样本对账、主键重复检查、最新水位和缺失率报告；
- 单位在 schema 注释、SQL 白名单和用户展示中一致；
- ETF 未复权行情用于成交，复权序列用于收益，分红日前后无机械跳空；
- 曲线期限和收益率百分比单位明确，`10Y - 2Y` 以 bp 展示；
- 商品连续收益在换月日无未解释的合约价差跳跃；
- 同步可幂等重跑、按年度 / 日期断点续传。

### 9.2 PIT 与回测纪律

- 宏观观测在 `availableDate` 前查询不到；
- 月度数据不以所属月份月末作为默认已知日；
- 缺失发布时间时默认不可用于策略，除非有文档化的保守可得日规则；
- 回测不使用当前 ETF 名单回填历史资产池，不使用上市前数据；
- 所有配置方案包含交易成本、调仓频率和样本起止日期。

### 9.3 研究可用性

完成后至少能稳定回答并复现：

1. 沪深 300、5 年 / 10 年 / 30 年国债、黄金和海外权益的长期风险收益及相关性；
2. 国债曲线当前水平、斜率及其历史分位；
3. 不同久期债券 ETF 对收益率上行的敏感度差异；
4. 黄金收益与美国实际利率、人民币汇率的滚动关系；
5. 商品期限结构处于 backwardation 还是 contango；
6. 等权、逆波动、风险平价和趋势过滤在统一成本下的样本内外表现；
7. 任意宏观信号在回测日期为什么已经可知。

## 10. 实现清单（每个波次通用）

每次实际落库均须同步完成：

1. Tushare API 类型和最小字段封装；
2. Prisma schema + Prisma 生成的新 migration；
3. 幂等、可续传的 store sync；
4. 独立开发 / 排障脚本和生产 maintenance 接线；
5. 单元测试、真实小样本对账和 `audit:data`；
6. `read-only-sql.ts` 的 `SQL_TABLE_DOCS`（字段、单位、市场和 PIT 语义）；
7. Agent 工具边界和确定性查询示例；
8. 如有 UI，补中英文 i18n、帮助文档和真实 E2E；
9. 若字段进入因子研究，同步 Factor V2 数据字段目录、时间语义、PIT 测试和报告口径；
10. 更新本文完成状态和实际数据水位。

## 11. 立项门与待确认事项

以下问题必须在相应波次开始前解决：

- `etf_share_size` 的权限与字段质量是否满足规模研究；`yc_cb` 已确认无权限并由官方公开曲线替代；
- Tushare 债券指数是否包含可用于个人研究的总回报指数及相应授权；
- 政金债 / 信用债曲线和 DR007 / R007 采用何种稳定、合法的数据源；
- 宏观接口发生修订时，Tushare 是否保留旧版本；若不保留，本地从何时开始积累 vintage；
- 商品指数的编制和连续规则能否获得并固定版本；
- 商品 ETF 的成交额、规模和跟踪误差是否足以成为真实配置代理；
- 海外权益和黄金报告采用人民币未对冲还是汇率对冲口径；第一版默认人民币未对冲，并明确展示
  USD/CNH 影响。

只有权限、口径和样本验证通过的数据才进入实现波次；无法验证的接口保持“缺数据”，不使用抓取、
估算或错误代理悄悄补齐。

### 11.1 Phase 5 数据完成合同（2026-08-11）

Phase 5 优先复用 Tushare 已通过能力探针的接口，并把数据分为两种不可混算的频率：

- 日频市场驱动：境内权益、国债曲线、信用利差、USD/CNH、美国实际利率、黄金和商品；
- 月频宏观轴：增长、通胀、信用、流动性和外部环境。

国内宏观补充 M1/M2、社融和 Shibor；外部环境补充美国名义/实际利率与 USD/CNH；商品补充会员持仓、
连续收益和换月台账。政金债/信用债曲线是唯一尚未证明稳定来源的核心输入，必须先通过真实小样本、授权、
单位和 PIT 验收。DR007/R007、债券 ETF 历史披露久期和全球供需数据库不阻塞 V1：流动性先用可审计的
Shibor 与货币/社融数据；债券 ETF 只报告由关键期限收益率变化估计的敏感度，不把统计估计命名为披露久期。

每条新增序列必须同时进入 bootstrap、增量维护、`audit:data`、只读 SQL 说明、Agent 口径和数据水位报告。
无法获得真实历史 vintage 的宏观接口保留 `latest_value_backfill` 标签，只允许探索；系统从首次采集日起
持续积累真实 vintage，禁止用保守发布时间加最终值伪造严格 PIT 历史。
