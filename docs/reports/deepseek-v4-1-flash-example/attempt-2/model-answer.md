**结论：这次分析没有跑成，一个数字都不能报。** 嵌入式 Cell 的计算沙箱在本会话里不可用，三次提交都停在执行层，`status: error / errorCode: execution_failed / error: "Python sandbox closed the protocol"`，且 `outputs` 为空：

1. 版本 1（`01M2HEWFNECSN1SPKWS9SRX433`，Matplotlib Agg 直出 Figure）——无任何 stdout。
2. 版本 2（同一张卡 revision 2，已改为只用 `charts.line`、并去掉 Matplotlib/display）——同样零输出。
3. 一张最小探针卡（只 `import sys; print(...)`，无数据、无图、无统计）——同样失败。

连 `print(sum([1,2,3]))` 都执行不了，说明故障在运行时本身，不在我的分析代码或 `data.series` 调用上。因此：波动率、分位数、Pearson/Spearman、OLS alpha/beta、滚动相关性**一概没有算出**，`AUDIT_JSON=` 我也不能给——那正是你要求避免的臆造。

**已确认的事实（只用只读元数据查询，未经过沙箱）**：隔离库里这三条月末序列确实存在，`dataCoverage` 显示 index 月末价格表共 248 行；按代码分组为 A=000300.SH 85 行、B=000852.SH 82 行、C=000905.SH 81 行，首末均为 2018-12-31 与 2025-12-31。对照 2018-12…2025-12 共 85 个自然月，可得缺失月数 A=0、B=3、C=4（A 完整、B 缺 3 个月、C 缺 4 个月）。这只验证了"缺口真实存在、必须按缺失处理"，**不含**任何收益、波动或相关性数值。

**已就绪、等沙箱恢复即可复跑的设计**（卡 01M2HEWFNECSN1SPKWS9SRX433 的 revision 2 已保存完整代码）：按 85 个自然月完整网格对齐、保留 NaN 不回填；收益在完整网格上取一阶差分，跨缺失月的变化永不当作单月收益；样本波动率用 ddof=1 × √12，分位数 linear；A-B、A-C 各做 all / pre2023(≤2022-12) / post2023(≥2023-01) 三段的成对完整样本统计（n、Pearson、Spearman、含截距 OLS 的月 alpha 与 beta）；滚动窗口要求"连续 12 个自然月 + 12 个完整成对观测"，两图分别用起点归一为 100 的价格曲线和滚动相关性，窗口不足或缺失处输出为独立连续线段（真断裂而非插值），图内英文标签。唯一需要你知情的取舍：版本 1 的 Matplotlib 直出方式已被我放弃，改为原生 `charts.line` 传显式线段列——官方静态图路径在该运行时也一并不可用（`charts.figure`/`charts.image` 不在目录中）。

策略代码保持原样未动，也没有发起任何回测。请稍后重试该 Cell；一旦沙箱可执行，表中数字、两张图与末尾一行精确 `AUDIT_JSON=` 会一并产出，我再据此解读 B 对 A 的分散是否被全样本掩盖、以及 C 与 A 的关系差异——在这些数字真实产出之前，我不想用任何替代来源替它们说话。