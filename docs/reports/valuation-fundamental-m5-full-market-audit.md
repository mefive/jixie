# M5 全市场补齐后的验收停点

日期：2026-09-06。状态：**异常核查与针对性防护已于 `63688994` 提交**。其余 M5 收尾见
[M5 复用与行业驱动评估](./valuation-fundamental-m5-reuse-and-drivers.md)。

前半部分保留首次停点的原始证据；下方“复核结果”记录后续更正。不能把首次规则命中直接解释为源数据错误。

## 本次操作

调用现有 `auditFinancialStatementAccounting(prisma)`，并只读汇总来源版本质量、异常类型和少量样本。
没有重新同步，没有修改财报、日期、版本选择或计算口径。

## 覆盖与历史可用性

| 表 | conservative | reconstructed | 合计 |
| --- | ---: | ---: | ---: |
| 利润表 | 448,022 | 38,320 | 486,342 |
| 资产负债表 | 387,930 | 6,741 | 394,671 |
| 现金流量表 | 407,660 | 59,461 | 467,121 |

- 共 1,348,134 个来源版本；没有 `exact` 版本。
- `conservative` 使用现有保守可用日期规则，并不代表掌握了精确发布时间。
- `reconstructed` 仍排除在严格 PIT 选择之外，不能因为完成补齐就视为过去已知。
- 非 reconstructed 记录覆盖 229,503 个“公司 × 报告期”，其中 222,041 个三表齐全，占 96.75%。
- 上述比例不是全市场股票覆盖率，也不是任意历史时点的可用率；未验证所有计算字段和连续季度均齐全。

## 现有审计返回 error

| 检查 | 不一致数 | 可比较数 |
| --- | ---: | ---: |
| 资产 = 负债 + 权益 | 114 | 394,338 |
| 期初现金 + 净增加额 = 期末现金 | 161 | 466,519 |
| 利润表与现金流量表净利润 | 1,483 | 319,290 |

另有 240 条记录触发符号或小计规则：资产非正 4 条、流动负债超过负债总额 103 条、购建长期资产现金支出为负 133 条。
后者包含 119 条 conservative、14 条 reconstructed。

这些是**待核查的规则命中**，不能直接认定为 240 条错误财报：负数可能需要会计解释；跨表比较也需要排除同日期多版本造成的组合误配。
计数是来源记录或比较组合数，不是受影响公司数，更不是用户最终选中版本的异常数。

### 本地样本

- `000738.SZ`，2026-06-30，reportType=1，conservative：流动负债 3,216,732,668.77 元，负债总额 3,017,788,205.16 元。
- `300352.SZ`，2026-03-31，reportType=1，conservative：购建长期资产现金支出 -3,784,779.76 元。

尚未逐项对照原始来源载荷和正式公告，不猜测以上差异的原因。

## 后续需确认的核查工作

1. 对上述异常分组、去重，并按 SDK 实际版本选择规则确认影响范围。
2. 对代表性记录追溯原始载荷及公司公告，区分源数据问题、映射问题和检查规则误报。
3. 给出保持原始版本不变的处理建议；改变口径或数据选择前再次人工审阅。

首次检查触发 review-gated-development 的数据语义停点；用户随后批准异常分类、必要的规则修复与测试。

## 复核结果

### 1. 从“所有来源行”改为“SDK 真正选中的版本”复核

新增只读 CLI：

```sh
pnpm --filter api audit:financial-selected 20260906
```

可在日期后追加股票代码做局部复核。脚本逐公司调用生产 `resolveFinancialState` 和指标内核，不复制一套 SQL 版本优先级，不更改数据库。

- 原库 6,403 个代码字符串中，`4920017.BJ`、`833243!1.BJ` 不符合当前 SDK 证券标识格式，明确列为排除，不猜测修正身份。
- 检查 6,401 个代码；45 个被现有金融行业规则排除。
- 截至 2026-09-06 选中 674,439 张报表，覆盖 227,758 个历史公司报告期，220,295 个三表齐全。
- 100,092 个“报表种类 × 公司 × 报告期”组折叠了等价来源版本。
- 293 个组存在同优先级不同值，仍由既有 resolver 拒绝选择；没有用 updateFlag 或同步先后猜版本。

| 实际选中版本上的检查 | 命中公司报告期数 | 处理 |
| --- | ---: | --- |
| 资产负债权益勾稽差异 | 85 | 暂停依赖该资产负债表的指标 |
| 现金期初、增量、期末勾稽差异 | 67 | 提示；当前指标不直接依赖这些余额，不扩大拦截 |
| 两表净利润差异 | 1,128 | 暂停依赖该净利润的 ROA、应计比率和现金利润比 |
| 负现金资本支出 | 68 | 暂停相关再投资、FCFF、现金自由现金流，等待解释 |
| 负非流动负债可解释小计关系 | 63 | 提示，不判算术错误、不拦截 |
| 无法解释的流动负债超过总负债 | 1 | 暂停相关营运资本及再投资链 |
| 总资产非正 | 3 | 暂停依赖资产分母的指标 |

历史范围内 904 家公司至少触发一项提示，不等于 904 家整体不可用。12,108 个“报告期 × 指标”值被本次规则标记为不可用。
这些统计包含旧报告期，**不是今天选股池的缺失率**，也不是已经逐条确认的错误数。M6 仍需在其预注册样本与历史切片上审计覆盖和剔除偏差。

### 2. 对照官方公告与来源接口的样本

**航发控制：原先“必然错误”的判断不成立。**
本地值、2026-09-06 只读 Tushare 抽查和 [2026 半年报](https://static.cninfo.com.cn/finalpage/2026-08-20/1225483676.PDF) 第 36 页一致：流动负债 3,216,732,668.77 元，非流动负债 -198,944,463.61 元，相加恰为总负债。
第 123 页列示负数专项应付款；第 12 页描述了项目验收、科研项目占用变动。规则改为“可对账的负数披露提示”，没有写死公司白名单。

**北信源：负资本支出确实写在公告中，但不推测其经营含义。**
本地值、只读 Tushare 抽查和 [2026 一季报](https://static.cninfo.com.cn/finalpage/2026-04-30/1225266287.PDF) 第 10 页均为 -3,784,779.76 元。不能称之为接口转错正负号；当前材料不足以将其认定为正常可持续再投资，相关指标暂不可用，收入和经营现金流仍可读取。

**得利斯：公告原文也存在无法对上的小计，不能自行修数。**
[2016 半年报](https://static.cninfo.com.cn/finalpage/2016-08-22/1202596882.PDF) 第 34 页同时列示流动负债 379,765,443.00 元、非流动负债 1,357,599.94 元、总负债 379,681,503.55 元，与本地值一致。三者不满足加总关系；未找到可据以决定应改哪一项的证据，保持记录不变并隔离相关计算。

上述三份公告的相关表格均已渲染并目视核对。其余勾稽差异没有批量认定为源错误：例如深华发 2016Q1 的资产负债表内部不一致、苏常柴 2017 年报的现金勾稽差异、宁德时代 2017Q1 的跨表净利润差异仍属待解释问题。

### 3. 实际代码产出及用户入口

- 内部模块 `apps/api/src/fundamentals/accounting-quality.ts`：对已选版本分类、指出相关指标。
- `calculateFinancialMetrics`：根据输入版本血缘传播限制，包括 TTM 所用季度、上年资产负债表；原始 state 和数据库不变。
- 公开 `data.equity_financial_metrics(...)` 沿用现有列返回 `value=null`、`status=invalid`、`missing_reason=accounting_review_required:…`，普通用户无需运行审计脚本即可看到结果。
- `data.equity_financial_statements(...)` 仍返回原始选中值，用于调查；不是经过清洗的可直接回测指标。
- 公式版本升至 `financial-metrics-v2`，避免新旧处理口径共用指纹语义；没有改变 PIT 日期规则、schema 或 SDK 返回列。
- 模板在最新候选报告缺少必要指标时明确停止，不偷偷回退至旧年报。可选的历史 FCFF 仍用明确不可用状态表达。
- 全库旧审计保留原始来源行/连接组合口径，但明确标注不是 SDK 最终选中版本；负数/小计提示与非正资产、股本区分。
- 中英文 FCFF 帮助增加异常状态说明。没有新增页面、数据修正表或人工“放行”开关。

### 4. 三公司影响与验证

五粮液、美的、宁德时代在既定估值日和下一年报复查日均通过三公司完整文档 E2E，三情景与反解结果和修改前一致。
宁德时代旧的 2015/2016 年现金流版本歧义继续排除，2017Q1 的跨表差异被标记；这些记录不参与案例当前估值输入，不需为了跑通案例而放宽规则。

本次异常处理不是全库清洗完成证明。其余 M5 helper 边界复查、分部数据探针收尾和总体验收文档仍按原计划处理；M6 未开始。

### 5. 本轮验证记录

- API 全量：181 个测试文件通过，937 项测试通过；1 个已有账户集成测试跳过（非本次财务研究范围）。
- 新增/扩展用例覆盖：负资本支出隔离、TTM 和上年余额血缘、无关指标可用、原始输入不变、可解释负负债小计、未解释小计、跨表净利润、现金勾稽提示、公开 SDK 状态透传和禁止回退旧年报。
- 全仓 `pnpm typecheck`、`pnpm build` 通过，包括 Research Runtime / Research SDK / Factor SDK 生成物一致性检查；构建保留现有大 chunk 警告。
- 本轮修改的 TypeScript ESLint、`git diff --check` 通过。
- `test:e2e:research-fcff-reuse`：3 家公司各完成 16 Cell，3 情景、反解和 4 项下一年报复查。
- `test:e2e:research-fcff-valuation`：16 Cell、3 情景、唯一反解、4 项复查、stale 与封存通过；执行 ID `01M1TGMQJ76HJQX9MWD4M8X1JN`。
- 截图：`apps/web/acceptance/research-fcff-reuse-midea-review.png` 等（验收输出，不入库为业务数据）。
- 不运行迁移、重同步、数据修复或新一轮备份：本轮没有 schema 及原始财报写入。

新创建的模板采用“不回退旧年报”逻辑；已保存的用户文档不批量改写，其自定义 Python 仍需用户审阅。

### 6. 本次提交边界

本次单独提交异常处理、对应测试、审计命令和帮助文档；尚未收尾的 M5 估值 helper、三案例配置及主营业务探针保留在工作区。
上面的全量测试和三公司 E2E 是完整工作区的回归证据，包含这些待提交改动，不应解释为独立提交快照的三公司验收。
拆分后额外直接执行暂存区中的模板选择代码，验证“最新年报异常时拒绝回退”和“最新年报有效时正确选中”均通过。
