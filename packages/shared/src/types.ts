/** Tushare-style stock code, e.g. '000001.SZ' (Shenzhen) / '600000.SH' (Shanghai) / '430047.BJ' (Beijing) */
export type TsCode = string;

/** Trade date, format YYYYMMDD, e.g. '20240131' */
export type TradeDate = string;
